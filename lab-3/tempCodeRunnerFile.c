rintf("%s\n", s[i]);
        // printf("isOperator: %d\n", isOperator(s[i]));
        // if (isOperator(s[i]) == 1) {
        //     double st = pop(arr);
        //     double ft = pop(arr);

        //     push(arr, getAns(ft, st, s[i]));
        // }
        // else {
        //     push(arr, s[i] - '0');
        